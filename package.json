{
  "name": "game",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node tools/check-assets.mjs",
    "build:lifeform": "bash tools/build-lifeform.sh",
    "predeploy": "npm run check"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
